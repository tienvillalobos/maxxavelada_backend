# Flask app (stats / API)

App mínima con DB dummy y ruta Hello World.

## Cómo ejecutar

```bash
cd flask_app
pip install -r requirements.txt
python app.py
```

Abre http://127.0.0.1:5000/ y verás "Hello World".
